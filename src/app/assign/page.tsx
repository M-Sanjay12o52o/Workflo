import Taskmodal from '@/components/Taskmodal'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <Taskmodal />
    </div>
}

export default page