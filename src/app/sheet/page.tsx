import { SheetSide } from '@/components/SheetPage'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <SheetSide />
    </div>
}

export default page